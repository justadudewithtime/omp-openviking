# The vendored tree must round-trip byte for byte, and the patches in
# patches/ are byte-exact unified diffs. Disable all end-of-line conversion so
# a clone on Windows (core.autocrlf=true) still matches the upstream tarball.
* -text
