# The patches in patches/ are byte-exact unified diffs against the upstream
# submodule checkout. Disable all end-of-line conversion so a clone on Windows
# (core.autocrlf=true) still applies them unchanged.
* -text
